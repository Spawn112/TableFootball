theme.leaderboard = {
    variable1 : '',//this is an example of a variable
    init : function() {
        //set the initial state and things related to this component (see this as a constructor)
        theme.leaderboard.method1();//this is just to illustrate how you can call methods, so delete when no longer needed
    },
    method1 : function() {
        //delete this at some point, but this is to illustrate how you can add methods to this "class"
    }
 }
 theme.leaderboard.init();